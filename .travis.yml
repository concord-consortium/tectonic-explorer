language: node_js
node_js:
  - 6
cache:
  yarn: true
  directories:
    - node_modules
install:
  - travis_retry yarn
  - travis_retry gem install s3_website -v 3.1.0
before_script: npm test && npm build
script: deploy/s3_deploy.sh
cache:
  directories:
    - node_modules
