import * as THREE from 'three'

// It can be imported by any class and updated. Then it's automatically passed to UI thread and rendered as red dot.
const debugMarker = new THREE.Vector3()
export default debugMarker
